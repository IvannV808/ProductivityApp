<Window x:Class="ProductivityApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Productivity App" Height="760" Width="1180" MinHeight="620" MinWidth="920"
        Background="#F6F7F9">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="18,18,18,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D3D8E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#DDE2EA" BorderThickness="0,0,1,0">
            <StackPanel VerticalAlignment="Top" Margin="0,42,0,0">
                <Button Content="App Config" Style="{StaticResource NavButtonStyle}" Click="AppConfigButton_Click"/>
                <Button Content="Metrics" Style="{StaticResource NavButtonStyle}" Margin="18,86,18,0" Click="MetricsButton_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="24,22,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Margin="0,0,0,14">
                <TextBlock Text="Weekly Schedule" FontSize="28" FontWeight="SemiBold" Foreground="#242A31"/>
                <TextBlock x:Name="StatusText" DockPanel.Dock="Right" HorizontalAlignment="Right"
                           VerticalAlignment="Center" Foreground="#657080" FontSize="13"/>
            </DockPanel>

            <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#DDE2EA" BorderThickness="1">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="ScheduleGrid"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
